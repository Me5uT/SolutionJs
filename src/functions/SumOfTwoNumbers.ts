export const sumOfTwoNumbers = (
  firstNumber: number,
  secondNumber: number
): number => {
  /**
   *  Returns sum of given two numbers.
   * @param firstNumber A number.
   * @param secondNumber A number.
   *
   */

  return firstNumber + secondNumber;
};
