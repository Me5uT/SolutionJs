export const percentageOfNumber = (num: number, percent: number): number => {
  /**
   *  Return the given percentage of the number.
   * @param num A number.
   * @param percent A number.
   */
  return (num * percent) / 100;
};
